#version 430 core

layout(local_size_x=1) in;

layout(std430, binding=0) buffer _input{
	float IN[];
};

layout(std430, binding=1) buffer _output{
	float OUT[];
};

void main(void){
	uint i=gl_GlobalInvocationID.x; float s=OUT[i];
	OUT[i]=IN[i]*s*(1-s);
}
